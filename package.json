{
  "name": "@bashbop/otito",
  "version": "1.9.0",
  "mcpName": "io.github.BASHBOP/otito",
  "description": "Òtítọ́: deterministic local repository context, impact analysis, and merge evidence.",
  "type": "module",
  "bin": {
    "otito": "src/cli.js"
  },
  "files": [
    "assets",
    "evals",
    "src",
    "scripts",
    "CHANGELOG.md",
    "LICENSE",
    "README.md",
    "SECURITY.md"
  ],
  "scripts": {
    "start": "node src/cli.js",
    "postinstall": "node scripts/postinstall.mjs",
    "mcp": "node src/cli.js mcp",
    "doctor": "node src/cli.js doctor",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "version:check": "node scripts/check-version.js",
    "version": "node scripts/sync-server-version.mjs && git add server.json",
    "test": "node --test tests/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include=\"src/**/*.js\" --test-coverage-lines=70 --test-coverage-branches=60 --test-coverage-functions=75 tests/*.test.js",
    "audit": "npm audit --omit=dev",
    "benchmark:cli": "node scripts/benchmark-cli.mjs",
    "smoke": "node src/cli.js doctor && node src/cli.js harness . --json && npm run smoke:tarball",
    "quality": "npm run format:check && npm run lint && npm run typecheck && npm run version:check && npm test && npm run test:coverage && npm run eval:accuracy && npm run eval:harness && npm run eval:gate && npm run audit && npm run smoke",
    "ci": "npm run quality",
    "prepublishOnly": "npm run ci",
    "smoke:tarball": "node scripts/smoke-tarball.mjs",
    "eval:accuracy": "node src/cli.js eval --accuracy",
    "eval:harness": "node src/cli.js eval --harness",
    "eval:gate": "node src/cli.js eval --gate-effectiveness"
  },
  "engines": {
    "node": ">=18.18"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BASHBOP/otito.git"
  },
  "homepage": "https://bashbop.github.io/otito/",
  "bugs": {
    "url": "https://github.com/BASHBOP/otito/issues"
  },
  "keywords": [
    "cli",
    "mcp",
    "mcp-server",
    "code-context",
    "code-map",
    "agent",
    "pr-review",
    "merge-gate",
    "repository-analysis",
    "developer-tools"
  ],
  "license": "MIT",
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "prettier": "^3.8.3"
  }
}
